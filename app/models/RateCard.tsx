export type BusinessModel = {
    id: number;
    title: string;
};

export type ServiceCategory = {
    id: number;
    title: string;
};

export type ServiceArea = {
    id: number;
    title: string;
};
